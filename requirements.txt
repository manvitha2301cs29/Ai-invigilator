# ==============================================================================
# AI STUDY INVIGILATOR -- COMBINED REQUIREMENTS (all phases, one shared venv)
# Put this file at the project root (same level as phase_a/, phase_b/, ...)
# ==============================================================================

# --- shared / core ---
numpy>=1.26.0
pytest>=8.0.0
pytest-cov>=5.0.0

# --- phase_a / phase_c watcher (webcam, perception) ---
opencv-python>=4.9.0
mediapipe>=0.10.14
requests>=2.31.0

# --- phase_c backend / phase_g backend (API + DB) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# --- phase_d / phase_e (PyTorch models) ---
torch>=2.2
matplotlib>=3.8

# --- phase_g backend only (LLM + auth + websockets) ---
anthropic>=0.34.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
websockets>=12.0
httpx>=0.27.0

# --- phase_h (packaging) ---
pyinstaller>=6.0
